{
  "name": "kata-template",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "scripts": {
    "jest": "yarn jest:unit",
    "jest:unit": "jest --config jest.unit.config.ts",
    "vitest": "yarn vitest:unit",
    "vitest:unit": "vitest --run --config vite.unit.config.ts",
    "test:all": "yarn jest && yarn vitest"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "vitest": "^0.34.6"
  }
}
